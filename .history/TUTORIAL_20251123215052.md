# Mac OS Portfolio Tutorial - Complete Guide

## 📋 BUILD CHECKLIST - Step by Step

> **Follow this checklist to build your portfolio systematically. Check off each item as you complete it!**

### ⚙️ Phase 0: Environment Setup (30 mins)
- [ ] Install Node.js (v18 or higher)
- [ ] Install VS Code or WebStorm
- [ ] Create project directory: `mkdir macos-portfolio && cd macos-portfolio`
- [ ] Initialize Vite: `npm create vite@latest . -- --template react`
- [ ] Install dependencies: `npm install`
- [ ] Test dev server: `npm run dev` (should open on localhost:5173)
- [ ] Install all required packages (see detailed list below)
- [ ] Configure Tailwind CSS
- [ ] Setup path aliases in `vite.config.js` and `jsconfig.json`
- [ ] Create folder structure: `components/`, `windows/`, `store/`, `hoc/`, `constants/`

### 🎨 Phase 1: Assets & Styling (15 mins)
- [ ] Download assets package (icons, images, wallpapers)
- [ ] Create `/public/images/` folder
- [ ] Create `/public/files/` folder
- [ ] Upload resume PDF to `/public/files/resume.pdf`
- [ ] Add wallpaper image to `/public/images/`
- [ ] Copy provided `index.css` styles
- [ ] Update `index.html` with your name and favicon
- [ ] Test: Background wallpaper should be visible

### 🔧 Phase 2: Constants & Data (30 mins)
- [ ] Create `src/constants/index.js`
- [ ] Add navigation links (`navLinks`, `navIcons`)
- [ ] Add dock apps configuration (`dockApps`)
- [ ] Add window configuration (`windowConfig`, `INITIAL_ZINDEX`)
- [ ] Add your tech stack (`techStack`)
- [ ] Add blog posts (`blogPosts`) - or placeholder data
- [ ] Add projects/work locations (`locations`)
- [ ] Add social links (`socials`)
- [ ] Add gallery images (`gallery`)
- [ ] **IMPORTANT:** Customize all data with YOUR information

### 🏪 Phase 3: State Management (20 mins)
- [ ] Create `src/store/window.js`
- [ ] Implement `useWindowStore` with Zustand + Immer
- [ ] Add `openWindow`, `closeWindow`, `focusWindow` functions
- [ ] Test: `console.log(useWindowStore.getState())` should work
- [ ] Create `src/store/location.js`
- [ ] Implement `useLocationStore`
- [ ] Add `setActiveLocation`, `resetActiveLocation` functions
- [ ] Export both stores

### 🎯 Phase 4: Core Components (1 hour)
- [ ] Create `src/components/index.js` for exports
- [ ] **Build Navbar** (`src/components/Navbar.jsx`)
  - [ ] Add logo, name, time display
  - [ ] Import from constants
  - [ ] Test: Navbar appears at top
- [ ] **Build Welcome** (`src/components/Welcome.jsx`)
  - [ ] Implement text rendering function
  - [ ] Add GSAP hover animations
  - [ ] Setup font weight transitions
  - [ ] Test: Text animates on hover
- [ ] **Build Dock** (`src/components/Dock.jsx`)
  - [ ] Add dock icons from constants
  - [ ] Implement GSAP magnification effect
  - [ ] Add tooltips with react-tooltip
  - [ ] Test: Icons magnify on hover
- [ ] **Build WindowControls** (`src/components/WindowControls.jsx`)
  - [ ] Add close/minimize/maximize buttons
  - [ ] Connect to window store
  - [ ] Test: Buttons render correctly

### 🪟 Phase 5: Window Wrapper HOC (30 mins)
- [ ] Create `src/hoc/WindowWrapper.jsx`
- [ ] Register GSAP Draggable plugin
- [ ] Implement wrapper function
- [ ] Add open/close animations
- [ ] Add draggable functionality
- [ ] Add focus on click
- [ ] Add z-index management
- [ ] Test: Create test window and verify it's draggable

### 💼 Phase 6: Build Windows (2-3 hours)

#### Terminal Window (Skills)
- [ ] Create `src/windows/Terminal.jsx`
- [ ] Add window header with controls
- [ ] Map over tech stack from constants
- [ ] Add terminal styling
- [ ] Wrap with WindowWrapper
- [ ] Export from `src/windows/index.js`
- [ ] Import in `App.jsx`
- [ ] Test: Click dock icon, window opens and is draggable

#### Safari Window (Blog)
- [ ] Create `src/windows/Safari.jsx`
- [ ] Add browser UI (address bar, buttons)
- [ ] Map over blog posts
- [ ] Add blog post cards
- [ ] Wrap with WindowWrapper
- [ ] Export and import in App
- [ ] Test: Opens, closes, shows blog posts

#### Resume Window
- [ ] Create `src/windows/Resume.jsx`
- [ ] Install react-pdf: `npm install react-pdf`
- [ ] Configure PDF.js worker
- [ ] Add Document and Page components
- [ ] Add download button
- [ ] Wrap with WindowWrapper
- [ ] Test: PDF displays correctly

#### Contact Window
- [ ] Create `src/windows/Contact.jsx`
- [ ] Add profile image
- [ ] Add email address
- [ ] Map over social links
- [ ] Style social buttons
- [ ] Wrap with WindowWrapper
- [ ] Test: All links work

#### Finder Window (Most Complex!)
- [ ] Create `src/windows/Finder.jsx`
- [ ] Use location store
- [ ] Add sidebar with favorites
- [ ] Add main content area
- [ ] Implement `renderList` function
- [ ] Implement `openItem` function
- [ ] Handle folders, PDFs, URLs, text, images
- [ ] Wrap with WindowWrapper
- [ ] Test: Navigate between folders, open files

#### Text Viewer Window
- [ ] Create `src/windows/Text.jsx`
- [ ] Get data from window store
- [ ] Display name, image, subtitle, description
- [ ] Wrap with WindowWrapper
- [ ] Test: Click .txt file in Finder, should open

#### Image Viewer Window
- [ ] Create `src/windows/Image.jsx`
- [ ] Get data from window store
- [ ] Display image
- [ ] Wrap with WindowWrapper
- [ ] Test: Click .png/.jpg file, should open

#### Photos/Gallery Window
- [ ] Create `src/windows/Photos.jsx`
- [ ] Add sidebar with photo categories
- [ ] Grid layout for images
- [ ] Click opens in Image viewer
- [ ] Wrap with WindowWrapper
- [ ] Test: Browse gallery, click opens full view

### 🏠 Phase 7: Desktop Folders (30 mins)
- [ ] Create `src/components/Home.jsx`
- [ ] Map over projects from locations
- [ ] Make folders draggable with GSAP
- [ ] Add click handler to open Finder
- [ ] Position folders on desktop
- [ ] Export and import in App
- [ ] Test: Drag folders, click opens in Finder

### 🔗 Phase 8: App Assembly (15 mins)
- [ ] Open `src/App.jsx`
- [ ] Import all components and windows
- [ ] Register GSAP Draggable
- [ ] Render in correct order:
  - [ ] Navbar
  - [ ] Welcome
  - [ ] All Windows
  - [ ] Home (desktop folders)
  - [ ] Dock
- [ ] Test: Everything renders and works together

### 🧪 Phase 9: Testing & Debugging (1 hour)
- [ ] Test all windows open/close
- [ ] Test dragging all windows
- [ ] Test window focus/z-index
- [ ] Test Finder navigation
- [ ] Test all file types open correctly
- [ ] Test dock magnification
- [ ] Test welcome text animation
- [ ] Test resume PDF loads
- [ ] Test all external links
- [ ] Test responsive warning message
- [ ] Check console for errors
- [ ] Fix any bugs found

### 🎨 Phase 10: Customization (1-2 hours)
- [ ] Replace all placeholder text with YOUR info
- [ ] Update name in Navbar
- [ ] Update name in Welcome screen
- [ ] Update profile photo in Contact
- [ ] Update email address
- [ ] Update social media links
- [ ] Add YOUR projects to locations
- [ ] Add YOUR tech stack
- [ ] Add YOUR blog posts (or remove Safari if none)
- [ ] Update resume PDF
- [ ] Add project screenshots/images
- [ ] Choose wallpaper
- [ ] Test all personalized content

### 🚀 Phase 11: Production Build & Deploy (30 mins)
- [ ] Run `npm run build`
- [ ] Check `dist` folder is created
- [ ] Test locally: `npm run preview`
- [ ] Setup Hostinger (or Vercel/Netlify)
- [ ] Upload files from `dist` folder
- [ ] Configure domain
- [ ] Test deployed site
- [ ] Fix any production issues
- [ ] Test resume PDF in production
- [ ] Test all links in production
- [ ] Check mobile responsiveness message

### ✅ Phase 12: Final Polish (30 mins)
- [ ] Add meta tags for SEO
- [ ] Add Open Graph tags for social sharing
- [ ] Optimize images (compress if large)
- [ ] Add favicon
- [ ] Test performance (Lighthouse)
- [ ] Share with friends for feedback
- [ ] Make final adjustments
- [ ] Update GitHub README
- [ ] Add to LinkedIn profile
- [ ] Share on social media

---

## 📊 Time Estimate

- **Beginner:** 10-12 hours
- **Intermediate:** 6-8 hours  
- **Advanced:** 4-5 hours

**Recommended Approach:** Build over 2-3 days, one phase at a time. Don't rush!

---

## 🎯 Project Overview

Build an interactive Mac OS-inspired desktop environment that serves as your professional portfolio. This isn't just another portfolio website—it's a full operating system experience that will make you stand out.

## ✨ Features

- **Fully Interactive Mac OS Desktop** - Complete with animated welcome screen
- **Magnifying Dock with Real Physics** - Smooth GSAP-powered animations
- **Multiple App Windows:**
  - 📁 Finder (Portfolio/Projects viewer)
  - 🌐 Safari (Developer blog)
  - 🖼️ Photos (Image gallery)
  - 📧 Contact (Social links)
  - 💻 Terminal (Tech stack display)
  - 📄 Resume Viewer (with download)
- **Dragable Windows** - True-to-life window management
- **File Viewers** - Text and image file support
- **Advanced Animations** - Premium GSAP interactions

## 🛠️ Tech Stack

- **Frontend:** React + Vite
- **Styling:** Tailwind CSS
- **Animations:** GSAP
- **State Management:** Zustand + Immer
- **UI Components:** Lucide React Icons
- **PDF Viewer:** React-PDF
- **Date/Time:** Day.js
- **Hosting:** Hostinger

## 📋 Prerequisites

- Node.js installed
- Basic knowledge of React
- Code editor (WebStorm recommended, free for non-commercial use)
- GitHub account
- Hostinger account (optional, for deployment)

---

## 🚀 Setup & Installation

### 1. Initialize Project

```bash
# Create project directory
mkdir macos-portfolio
cd macos-portfolio

# Initialize Vite with React
npm create vite@latest . -- --template react

# Install dependencies
npm install
```

### 2. Install Required Packages

```bash
# Core dependencies
npm install tailwindcss@latest
npm install @tailwindcss/vit
npm install gsap @gsap/react
npm install zustand immer
npm install lucide-react
npm install react-tooltip
npm install react-pdf
npm install dayjs
npm install clsx

# GSAP Draggable plugin
npm install gsap/draggable
```

### 3. Configure Tailwind CSS

Create `tailwind.config.js`:

```bash
npx tailwindcss init
```

Update `vite.config.js`:

```javascript
import { defineConfig } from "vite";
import react from "@vitejs/plugin-react";
import tailwindcss from "@tailwindcss/vit";
import { resolve } from "path";
import { fileURLToPath } from "url";

export default defineConfig({
  plugins: [react(), tailwindcss()],
  resolve: {
    alias: {
      "@components": resolve(
        fileURLToPath(import.meta.url),
        "../src/components"
      ),
      "@constants": resolve(fileURLToPath(import.meta.url), "../src/constants"),
      "@store": resolve(fileURLToPath(import.meta.url), "../src/store"),
      "@hoc": resolve(fileURLToPath(import.meta.url), "../src/hoc"),
      "@windows": resolve(fileURLToPath(import.meta.url), "../src/windows"),
    },
  },
});
```

Create `jsconfig.json`:

```json
{
  "compilerOptions": {
    "baseUrl": ".",
    "paths": {
      "#*": ["src/*"]
    }
  },
  "include": ["src"]
}
```

### 4. Setup Project Structure

```
src/
├── components/       # Reusable UI components
├── windows/         # Window components (Finder, Safari, etc.)
├── hoc/            # Higher Order Components
├── store/          # Zustand stores
├── constants/      # Static data and configurations
└── assets/         # Images, icons, fonts

public/
├── images/         # Static images
└── files/          # Resume PDF
```

---

## 📁 Project Files Setup

### Assets & Styling

Download the complete assets package (includes icons, images, wallpapers) and place in `/public`.

Update `index.css` with the provided styles (includes fonts, theme variables, utility classes).

Update `index.html`:

```html
<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <link rel="icon" type="image/png" href="/macbook.png" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Your Name - Portfolio</title>
  </head>
  <body>
    <div id="root"></div>
    <script type="module" src="/src/main.jsx"></script>
  </body>
</html>
```

---

## 🏗️ Building the Portfolio

### Phase 1: Foundation

#### 1. Navbar Component

```jsx
// src/components/Navbar.jsx
import { navLinks, navIcons } from "@constants";
import dayjs from "dayjs";

const Navbar = () => {
  return (
    <nav>
      <div>
        <img src="/images/apple.svg" alt="logo" />
        <p className="font-bold">Your Name's Portfolio</p>
      </div>

      <ul>
        {navLinks.map(({ id, name }) => (
          <li key={id}>
            <p>{name}</p>
          </li>
        ))}
      </ul>

      <div>
        <ul>
          {navIcons.map(({ id, img }) => (
            <li key={id}>
              <img src={img} className="icon-hover" alt={`icon-${id}`} />
            </li>
          ))}
        </ul>
        <time>{dayjs().format("ddd MMM D H:mm A")}</time>
      </div>
    </nav>
  );
};

export default Navbar;
```

#### 2. Welcome Screen with Animated Text

```jsx
// src/components/Welcome.jsx
import { useRef } from "react";
import { useGSAP } from "@gsap/react";
import gsap from "gsap";

const FONT_WEIGHTS = {
  subtitle: { min: 100, max: 400, default: 100 },
  title: { min: 400, max: 900, default: 400 },
};

const renderText = (text, className, baseWeight = 400) => {
  return [...text].map((char, i) => (
    <span
      key={i}
      className={className}
      style={{ fontVariationSettings: `'wght' ${baseWeight}` }}
    >
      {char === " " ? "\u00A0" : char}
    </span>
  ));
};

const setupTextHover = (container, type) => {
  if (!container) return;

  const letters = container.querySelectorAll("span");
  const { min, max, default: base } = FONT_WEIGHTS[type];

  const animateLetter = (letter, weight, duration = 0.25) => {
    return gsap.to(letter, {
      duration,
      ease: "power2.out",
      fontVariationSettings: `'wght' ${weight}`,
    });
  };

  const handleMouseMove = (e) => {
    const { left } = container.getBoundingClientRect();
    const mouseX = e.clientX - left;

    letters.forEach((letter) => {
      const { left: l, width: w } = letter.getBoundingClientRect();
      const distance = Math.abs(mouseX - (l - left + w / 2));
      const intensity = Math.exp(-(distance ** 2) / 20000);

      animateLetter(letter, min + (max - min) * intensity);
    });
  };

  const handleMouseLeave = () => {
    letters.forEach((letter) => animateLetter(letter, base, 0.3));
  };

  container.addEventListener("mousemove", handleMouseMove);
  container.addEventListener("mouseleave", handleMouseLeave);

  return () => {
    container.removeEventListener("mousemove", handleMouseMove);
    container.removeEventListener("mouseleave", handleMouseLeave);
  };
};

const Welcome = () => {
  const titleRef = useRef(null);
  const subtitleRef = useRef(null);

  useGSAP(() => {
    const titleCleanup = setupTextHover(titleRef.current, "title");
    const subtitleCleanup = setupTextHover(subtitleRef.current, "subtitle");

    return () => {
      titleCleanup?.();
      subtitleCleanup?.();
    };
  }, []);

  return (
    <section id="welcome">
      <p ref={subtitleRef}>
        {renderText(
          "Hey, I'm Your Name. Welcome to my",
          "text-3xl font-georama",
          100
        )}
      </p>
      <h1 ref={titleRef} className="mt-7">
        {renderText("PORTFOLIO", "text-9xl italic font-georama", 400)}
      </h1>

      <div className="small-screen">
        <p>This portfolio is designed for desktop/tablet screens only</p>
      </div>
    </section>
  );
};

export default Welcome;
```

#### 3. Dock Component

```jsx
// src/components/Dock.jsx
import { useRef } from "react";
import { useGSAP } from "@gsap/react";
import gsap from "gsap";
import { Tooltip } from "react-tooltip";
import { dockApps } from "@constants";
import { useWindowStore } from "@store/window";

const Dock = () => {
  const dockRef = useRef(null);
  const { toggleApp } = useWindowStore();

  useGSAP(() => {
    const dock = dockRef.current;
    if (!dock) return;

    const icons = dock.querySelectorAll(".dock-icon");

    const animateIcons = (mouseX) => {
      const { left } = dock.getBoundingClientRect();

      icons.forEach((icon) => {
        const { left: iconLeft, width } = icon.getBoundingClientRect();
        const center = iconLeft - left + width / 2;
        const distance = Math.abs(mouseX - center);
        const intensity = Math.exp(-(distance ** 2) / 2.5);

        gsap.to(icon, {
          scale: 1 + 0.25 * intensity,
          y: -15 * intensity,
          duration: 0.2,
          ease: "power1.out",
        });
      });
    };

    const handleMouseMove = (e) => {
      const { left } = dock.getBoundingClientRect();
      animateIcons(e.clientX - left);
    };

    const resetIcons = () => {
      icons.forEach((icon) => {
        gsap.to(icon, {
          scale: 1,
          y: 0,
          duration: 0.3,
          ease: "power1.out",
        });
      });
    };

    dock.addEventListener("mousemove", handleMouseMove);
    dock.addEventListener("mouseleave", resetIcons);

    return () => {
      dock.removeEventListener("mousemove", handleMouseMove);
      dock.removeEventListener("mouseleave", resetIcons);
    };
  }, []);

  return (
    <section id="dock">
      <div ref={dockRef} className="dock-container">
        {dockApps.map(({ id, name, icon, canOpen }) => (
          <div key={id || name} className="relative flex justify-center">
            <button
              type="button"
              className="dock-icon"
              aria-label={name}
              data-tooltip-id="dock-tooltip"
              data-tooltip-content={name}
              data-tooltip-delay-show={150}
              disabled={!canOpen}
              onClick={() => toggleApp({ id, canOpen })}
            >
              <img
                src={`/images/${icon}`}
                alt={name}
                loading="lazy"
                className={!canOpen ? "opacity-60" : ""}
              />
            </button>
          </div>
        ))}
      </div>
      <Tooltip id="dock-tooltip" place="top" className="tooltip" />
    </section>
  );
};

export default Dock;
```

---

### Phase 2: State Management

#### 1. Windows Store (Zustand + Immer)

```jsx
// src/store/window.js
import { create } from "zustand";
import { immer } from "zustand/middleware/immer";
import { windowConfig, INITIAL_ZINDEX } from "@constants";

export const useWindowStore = create(
  immer((set) => ({
    windows: windowConfig,
    nextZIndex: INITIAL_ZINDEX + 1,

    openWindow: (windowKey, data = null) =>
      set((state) => {
        const win = state.windows[windowKey];
        if (!win) return;

        win.isOpen = true;
        win.zIndex = state.nextZIndex;
        win.data = data || win.data;
        state.nextZIndex++;
      }),

    closeWindow: (windowKey) =>
      set((state) => {
        const win = state.windows[windowKey];
        if (!win) return;

        win.isOpen = false;
        win.zIndex = INITIAL_ZINDEX;
        win.data = null;
      }),

    focusWindow: (windowKey) =>
      set((state) => {
        const win = state.windows[windowKey];
        if (!win) return;

        win.zIndex = state.nextZIndex++;
      }),
  }))
);
```

#### 2. Location Store

```jsx
// src/store/location.js
import { create } from "zustand";
import { immer } from "zustand/middleware/immer";
import { locations } from "@constants";

const DEFAULT_LOCATION = locations.work;

export const useLocationStore = create(
  immer((set) => ({
    activeLocation: DEFAULT_LOCATION,

    setActiveLocation: (location) =>
      set((state) => {
        if (location) {
          state.activeLocation = location;
        }
      }),

    resetActiveLocation: () =>
      set((state) => {
        state.activeLocation = DEFAULT_LOCATION;
      }),
  }))
);
```

---

### Phase 3: Higher Order Component (Window Wrapper)

```jsx
// src/hoc/WindowWrapper.jsx
import { useRef, useLayoutEffect } from "react";
import { useGSAP } from "@gsap/react";
import gsap from "gsap";
import Draggable from "gsap/Draggable";
import { useWindowStore } from "@store/window";

gsap.registerPlugin(Draggable);

const WindowWrapper = (Component, windowKey) => {
  const Wrapped = (props) => {
    const ref = useRef(null);
    const { focusWindow, windows } = useWindowStore();
    const { isOpen, zIndex } = windows[windowKey] || {};

    // Hide/show window based on isOpen state
    useLayoutEffect(() => {
      const el = ref.current;
      if (!el) return;

      el.style.display = isOpen ? "block" : "none";
    }, [isOpen]);

    // Opening animation
    useGSAP(() => {
      const el = ref.current;
      if (!el || !isOpen) return;

      el.style.display = "block";

      gsap.from(el, {
        scale: 0.8,
        opacity: 0,
        y: 40,
        duration: 0.4,
        ease: "power3.out",
      });
    }, [isOpen]);

    // Make window draggable
    useGSAP(() => {
      const el = ref.current;
      if (!el) return;

      const instance = Draggable.create(el, {
        onPress: () => focusWindow(windowKey),
      });

      return () => instance[0].kill();
    }, []);

    return (
      <section id={windowKey} ref={ref} style={{ zIndex }} className="absolute">
        <Component {...props} />
      </section>
    );
  };

  Wrapped.displayName = `WindowWrapper(${
    Component.displayName || Component.name || "Component"
  })`;

  return Wrapped;
};

export default WindowWrapper;
```

---

### Phase 4: Window Components

#### 1. Window Controls (Reusable)

```jsx
// src/components/WindowControls.jsx
import { useWindowStore } from "@store/window";

const WindowControls = ({ target }) => {
  const { closeWindow } = useWindowStore();

  return (
    <div id="window-controls">
      <div className="close" onClick={() => closeWindow(target)} />
      <div className="minimize" />
      <div className="maximize" />
    </div>
  );
};

export default WindowControls;
```

#### 2. Terminal Window (Skills)

```jsx
// src/windows/Terminal.jsx
import { Check, Flag } from "lucide-react";
import WindowWrapper from "@hoc/WindowWrapper";
import WindowControls from "@components/WindowControls";
import { techStack } from "@constants";

const Terminal = () => {
  return (
    <>
      <div id="window-header">
        <WindowControls target="terminal" />
        <h2>Tech Stack</h2>
      </div>

      <div className="tech-stack">
        <p>
          <span className="font-bold">@yourname % </span>
          show tech stack
        </p>

        <div className="label">
          <p className="w-32">Category</p>
          <p>Technologies</p>
        </div>

        <ul className="content">
          {techStack.map(({ category, items }) => (
            <li key={category} className="flex items-center">
              <Check className="check" size={20} />
              {category}
              <ul>
                {items.map((item, i) => (
                  <li key={i}>
                    {item}
                    {i < items.length - 1 ? ", " : ""}
                  </li>
                ))}
              </ul>
            </li>
          ))}
        </ul>

        <div className="footnote">
          <p>
            <Check size={20} />5 of 5 stacks loaded successfully (100%)
          </p>
          <p className="text-black">
            <Flag size={15} fill="black" />
            Render time: 6ms
          </p>
        </div>
      </div>
    </>
  );
};

const TerminalWindow = WindowWrapper(Terminal, "terminal");
export default TerminalWindow;
```

#### 3. Safari Window (Blog)

```jsx
// src/windows/Safari.jsx
import {
  PanelLeft,
  ChevronLeft,
  ChevronRight,
  ShieldHalf,
  Search,
  Share,
  Plus,
  Copy,
  MoveRight,
} from "lucide-react";
import WindowWrapper from "@hoc/WindowWrapper";
import WindowControls from "@components/WindowControls";
import { blogPosts } from "@constants";

const Safari = () => {
  return (
    <>
      <div id="window-header">
        <WindowControls target="safari" />
        <PanelLeft className="ml-10 icon" />

        <div className="flex items-center gap-1 ml-5">
          <ChevronLeft className="icon" />
          <ChevronRight className="icon" />
        </div>

        <div className="flex-1 flex-center gap-3">
          <ShieldHalf className="icon" />

          <div className="search">
            <Search className="icon" />
            <input
              type="text"
              placeholder="Search or enter website name"
              className="flex-1"
            />
          </div>
        </div>

        <div className="flex items-center gap-5">
          <Share className="icon" />
          <Plus className="icon" />
          <Copy className="icon" />
        </div>
      </div>

      <div className="blog">
        <h2>My Developer Blog</h2>

        <div className="space-y-4">
          {blogPosts.map(({ id, image, title, date, link }) => (
            <div key={id} className="blog-post">
              <div className="col-span-2">
                <img src={image} alt={title} />
              </div>

              <div className="content">
                <p>{date}</p>
                <h3>{title}</h3>
                <a href={link} target="_blank" rel="noopener noreferrer">
                  Check out the full post
                  <MoveRight className="icon-hover" />
                </a>
              </div>
            </div>
          ))}
        </div>
      </div>
    </>
  );
};

const SafariWindow = WindowWrapper(Safari, "safari");
export default SafariWindow;
```

#### 4. Resume Viewer

```jsx
// src/windows/Resume.jsx
import { Download } from "lucide-react";
import { Document, Page, pdfjs } from "react-pdf";
import "react-pdf/dist/Page/AnnotationLayer.css";
import "react-pdf/dist/Page/TextLayer.css";
import WindowWrapper from "@hoc/WindowWrapper";
import WindowControls from "@components/WindowControls";

// Configure PDF.js worker
pdfjs.GlobalWorkerOptions.workerSrc = `https://unpkg.com/pdfjs-dist@${pdfjs.version}/build/pdf.worker.min.mjs`;

const Resume = () => {
  return (
    <>
      <div id="window-header">
        <WindowControls target="resume" />
        <h2>resume.pdf</h2>

        <a
          href="/files/resume.pdf"
          download
          className="cursor-pointer"
          title="Download Resume"
        >
          <Download className="icon" />
        </a>
      </div>

      <Document file="/files/resume.pdf">
        <Page pageNumber={1} renderTextLayer renderAnnotationLayer />
      </Document>
    </>
  );
};

const ResumeWindow = WindowWrapper(Resume, "resume");
export default ResumeWindow;
```

#### 5. Contact Window

```jsx
// src/windows/Contact.jsx
import WindowWrapper from "@hoc/WindowWrapper";
import WindowControls from "@components/WindowControls";
import { socials } from "@constants";

const Contact = () => {
  return (
    <>
      <div id="window-header">
        <h2>Contact Me</h2>
        <WindowControls target="contact" />
      </div>

      <div className="p-5 space-y-5">
        <img
          src="/images/adrian.jpeg"
          alt="Your Name"
          className="w-20 rounded-full"
        />

        <h3>Let's Connect</h3>
        <p>Got an idea, a bug to squash, or just want to talk tech? I'm in.</p>
        <p>Email: your.email@example.com</p>

        <ul>
          {socials.map(({ id, bg, link, icon, text }) => (
            <li key={id} style={{ backgroundColor: bg }}>
              <a
                href={link}
                target="_blank"
                rel="noopener noreferrer"
                title={text}
              >
                <img src={icon} alt={text} className="size-5" />
                <p>{text}</p>
              </a>
            </li>
          ))}
        </ul>
      </div>
    </>
  );
};

const ContactWindow = WindowWrapper(Contact, "contact");
export default ContactWindow;
```

#### 6. Finder (Portfolio)

```jsx
// src/windows/Finder.jsx
import { Search } from "lucide-react";
import clsx from "clsx";
import WindowWrapper from "@hoc/WindowWrapper";
import WindowControls from "@components/WindowControls";
import { useLocationStore } from "@store/location";
import { useWindowStore } from "@store/window";
import { locations } from "@constants";

const Finder = () => {
  const { activeLocation, setActiveLocation } = useLocationStore();
  const { openWindow } = useWindowStore();

  const renderList = (name, items) => (
    <div>
      <h3>{name}</h3>
      <ul>
        {items.map((item) => (
          <li
            key={item.id}
            className={clsx({
              active: item.id === activeLocation?.id,
              "not-active": item.id !== activeLocation?.id,
            })}
            onClick={() => setActiveLocation(item)}
          >
            <img src={item.icon} className="w-4" alt={item.name} />
            <p className="text-sm font-medium truncate">{item.name}</p>
          </li>
        ))}
      </ul>
    </div>
  );

  const openItem = (item) => {
    if (item.fileType === "pdf") {
      return openWindow("resume");
    }

    if (item.kind === "folder") {
      return setActiveLocation(item);
    }

    if (["fig", "url"].includes(item.fileType) && item.href) {
      return window.open(item.href, "_blank");
    }

    // Open text/image viewer
    openWindow(`${item.fileType}-${item.id}`, item);
  };

  return (
    <>
      <div id="window-header">
        <WindowControls target="finder" />
        <Search className="icon" />
      </div>

      <div className="bg-white flex h-full">
        <div className="sidebar">
          {renderList("Favorites", Object.values(locations))}
          {renderList("Work", locations.work.children)}
        </div>

        <ul className="content">
          {activeLocation?.children?.map((item) => (
            <li
              key={item.id}
              className={item.position}
              onClick={() => openItem(item)}
            >
              <img src={item.icon} alt={item.name} />
              <p>{item.name}</p>
            </li>
          ))}
        </ul>
      </div>
    </>
  );
};

const FinderWindow = WindowWrapper(Finder, "finder");
export default FinderWindow;
```

#### 7. Text File Viewer

```jsx
// src/windows/Text.jsx
import WindowWrapper from "@hoc/WindowWrapper";
import WindowControls from "@components/WindowControls";
import { useWindowStore } from "@store/window";

const Text = () => {
  const { windows } = useWindowStore();
  const data = windows["txt-file"]?.data;

  if (!data) return null;

  const { name, image, subtitle, description } = data;

  return (
    <>
      <div id="window-header">
        <WindowControls target="txt-file" />
        <h2>{name}</h2>
      </div>

      <div className="p-5">
        {image && <img src={image} alt={name} className="mb-4" />}
        {subtitle && <p className="text-lg font-bold mb-2">{subtitle}</p>}
        {description?.map((para, i) => (
          <p key={i} className="mb-2">
            {para}
          </p>
        ))}
      </div>
    </>
  );
};

const TextWindow = WindowWrapper(Text, "txt-file");
export default TextWindow;
```

#### 8. Image Viewer

```jsx
// src/windows/Image.jsx
import WindowWrapper from "@hoc/WindowWrapper";
import WindowControls from "@components/WindowControls";
import { useWindowStore } from "@store/window";

const Image = () => {
  const { windows } = useWindowStore();
  const data = windows["image-file"]?.data;

  if (!data) return null;

  const { name, imageUrl } = data;

  return (
    <>
      <div id="window-header">
        <WindowControls target="image-file" />
        <h2>{name}</h2>
      </div>

      <div className="p-5">
        <img src={imageUrl} alt={name} className="w-full" />
      </div>
    </>
  );
};

const ImageWindow = WindowWrapper(Image, "image-file");
export default ImageWindow;
```

#### 9. Photos/Gallery Window

```jsx
// src/windows/Photos.jsx
import WindowWrapper from "@hoc/WindowWrapper";
import WindowControls from "@components/WindowControls";
import { useWindowStore } from "@store/window";
import { photoLinks, gallery } from "@constants";

const Photos = () => {
  const { openWindow } = useWindowStore();

  return (
    <>
      <div id="window-header">
        <WindowControls target="photos" />
        <h2>Photos</h2>
      </div>

      <div className="flex h-full">
        <div className="sidebar">
          <h3>Library</h3>
          <ul>
            {photoLinks.map(({ id, name, icon }) => (
              <li key={id}>
                <img src={icon} alt={name} />
                <p>{name}</p>
              </li>
            ))}
          </ul>
        </div>

        <div className="grid grid-cols-3 gap-2 p-5">
          {gallery.map((photo) => (
            <img
              key={photo.id}
              src={photo.imageUrl}
              alt={photo.name}
              className="cursor-pointer hover:opacity-80"
              onClick={() => openWindow("image-file", photo)}
            />
          ))}
        </div>
      </div>
    </>
  );
};

const PhotosWindow = WindowWrapper(Photos, "photos");
export default PhotosWindow;
```

---

### Phase 5: Desktop Folders

```jsx
// src/components/Home.jsx
import { useGSAP } from "@gsap/react";
import Draggable from "gsap/Draggable";
import clsx from "clsx";
import { useWindowStore } from "@store/window";
import { useLocationStore } from "@store/location";
import { locations } from "@constants";

const Home = () => {
  const { openWindow } = useWindowStore();
  const { setActiveLocation } = useLocationStore();

  const projects = locations.work?.children || [];

  useGSAP(() => {
    Draggable.create(".folder");
  }, []);

  const handleOpenProjectFinder = (project) => {
    setActiveLocation(project);
    openWindow("finder");
  };

  return (
    <section id="home">
      <ul>
        {projects.map((project) => (
          <li
            key={project.id}
            className={clsx("group folder", project.windowPosition)}
            onClick={() => handleOpenProjectFinder(project)}
          >
            <img src="/images/folder.png" alt={project.name} />
            <p>{project.name}</p>
          </li>
        ))}
      </ul>
    </section>
  );
};

export default Home;
```

---

### Phase 6: Main App Assembly

```jsx
// src/App.jsx
import gsap from "gsap";
import Draggable from "gsap/Draggable";
import { Navbar, Welcome, Dock, Home } from "@components";
import {
  Terminal,
  Safari,
  Resume,
  Contact,
  Finder,
  Text,
  Image,
  Photos,
} from "@windows";

gsap.registerPlugin(Draggable);

function App() {
  return (
    <main>
      <Navbar />
      <Welcome />

      {/* Windows */}
      <Terminal />
      <Safari />
      <Resume />
      <Contact />
      <Finder />
      <Text />
      <Image />
      <Photos />

      {/* Desktop & Dock */}
      <Home />
      <Dock />
    </main>
  );
}

export default App;
```

---

## 📝 Constants Configuration

Create `src/constants/index.js` with all your data:

```javascript
// Navigation
export const navLinks = [
  { id: 1, name: "Portfolio", type: "finder" },
  { id: 2, name: "Contact", type: "contact" },
  { id: 3, name: "Resume", type: "resume" },
];

// Dock Apps
export const dockApps = [
  { id: "finder", name: "Portfolio", icon: "finder.png", canOpen: true },
  { id: "safari", name: "Articles", icon: "safari.png", canOpen: true },
  { id: "photos", name: "Gallery", icon: "photos.png", canOpen: true },
  { id: "contact", name: "Contact", icon: "mail.png", canOpen: true },
  { id: "terminal", name: "Skills", icon: "terminal.png", canOpen: true },
  { id: "trash", name: "Archive", icon: "trash.png", canOpen: false },
];

// Window Configuration
export const INITIAL_ZINDEX = 1000;

export const windowConfig = {
  finder: { isOpen: false, zIndex: INITIAL_ZINDEX, data: null },
  safari: { isOpen: false, zIndex: INITIAL_ZINDEX, data: null },
  terminal: { isOpen: false, zIndex: INITIAL_ZINDEX, data: null },
  resume: { isOpen: false, zIndex: INITIAL_ZINDEX, data: null },
  contact: { isOpen: false, zIndex: INITIAL_ZINDEX, data: null },
  photos: { isOpen: false, zIndex: INITIAL_ZINDEX, data: null },
  "txt-file": { isOpen: false, zIndex: INITIAL_ZINDEX, data: null },
  "image-file": { isOpen: false, zIndex: INITIAL_ZINDEX, data: null },
};

// Tech Stack
export const techStack = [
  { category: "Frontend", items: ["React", "Next.js", "TypeScript"] },
  { category: "Mobile", items: ["React Native", "Expo"] },
  { category: "Styling", items: ["Tailwind CSS", "CSS-in-JS"] },
  { category: "Backend", items: ["Node.js", "Express", "MongoDB"] },
  { category: "Tools", items: ["Git", "Docker", "VS Code"] },
];

// Blog Posts
export const blogPosts = [
  {
    id: 1,
    date: "2024-11-01",
    title: "Building a Mac OS Portfolio",
    image: "/images/blog1.jpg",
    link: "https://yourblog.com/post1",
  },
  // Add more posts...
];

// Projects/Work
export const locations = {
  work: {
    id: "work",
    name: "Work",
    icon: "/images/folder.png",
    kind: "folder",
    children: [
      {
        id: "project1",
        name: "Nike E-Commerce",
        icon: "/images/folder.png",
        kind: "folder",
        position: "top-[15vh] right-[30vw]",
        windowPosition: "top-[10vh] right-[25vw]",
        children: [
          {
            id: "p1-txt",
            name: "about.txt",
            kind: "file",
            fileType: "txt",
            position: "top-[5vh] left-[5vw]",
            data: {
              name: "Nike E-Commerce",
              subtitle: "Full-stack e-commerce platform",
              description: [
                "Built with React, Node.js, and MongoDB",
                "Features: Product catalog, cart, checkout",
                "Responsive design with Tailwind CSS",
              ],
            },
          },
          {
            id: "p1-url",
            name: "nike.com",
            kind: "file",
            fileType: "url",
            href: "https://nike-demo.com",
            position: "top-[5vh] left-[20vw]",
          },
          // Add more project files...
        ],
      },
      // Add more projects...
    ],
  },
  about: {
    id: "about",
    name: "About Me",
    icon: "/images/user.png",
    kind: "folder",
    children: [
      {
        id: "bio",
        name: "bio.txt",
        kind: "file",
        fileType: "txt",
        position: "top-[10vh] left-[10vw]",
        data: {
          name: "About Me",
          image: "/images/adrian.jpeg",
          description: [
            "Full-stack developer passionate about building great UX",
            "Specializing in React, Node.js, and modern web technologies",
          ],
        },
      },
    ],
  },
  resume: {
    id: "resume",
    name: "Resume",
    icon: "/images/pdf.png",
    kind: "file",
    fileType: "pdf",
  },
  trash: {
    id: "trash",
    name: "Trash",
    icon: "/images/trash.png",
    kind: "folder",
    children: [],
  },
};

// Social Links
export const socials = [
  {
    id: 1,
    text: "GitHub",
    icon: "/images/github.png",
    link: "https://github.com/yourusername",
    bg: "#000",
  },
  {
    id: 2,
    text: "LinkedIn",
    icon: "/images/linkedin.png",
    link: "https://linkedin.com/in/yourusername",
    bg: "#0077b5",
  },
  {
    id: 3,
    text: "Twitter",
    icon: "/images/twitter.png",
    link: "https://twitter.com/yourusername",
    bg: "#1da1f2",
  },
];

// Gallery
export const gallery = [
  {
    id: 1,
    name: "Project Screenshot 1",
    imageUrl: "/images/gallery1.jpg",
  },
  // Add more images...
];
```

---

## 🚀 Deployment

### Build for Production

```bash
npm run build
```

### Deploy to Hostinger

1. **Setup Hosting:**

   - Go to Hostinger dashboard
   - Create new website (empty HTML)
   - Choose domain name

2. **Upload Files:**

   - Navigate to File Manager
   - Go to `public_html`
   - Delete default files
   - Upload contents of `dist` folder

3. **Configure PDF Worker (if needed):**
   - Update Resume.jsx to use CDN worker source
   - Rebuild and redeploy

### Alternative Deployment Options

- **Vercel:** `npm i -g vercel && vercel`
- **Netlify:** Drag & drop `dist` folder
- **GitHub Pages:** Use `gh-pages` branch

---

## 🎨 Customization Guide

### 1. Personal Information

Update these files with your info:

- `src/constants/index.js` - All data
- `public/files/resume.pdf` - Your resume
- `public/images/` - Your photos

### 2. Colors & Styling

Modify `src/index.css`:

- CSS variables for theme colors
- Wallpaper background
- Window styles

### 3. Projects

Add/edit projects in `constants/index.js`:

```javascript
{
  id: 'your-project',
  name: 'Project Name',
  icon: '/images/folder.png',
  kind: 'folder',
  position: 'top-[20vh] right-[40vw]',
  children: [/* project files */]
}
```

### 4. Tech Stack

Update `techStack` array in constants with your technologies.

### 5. Blog Posts

Update `blogPosts` array with your articles.

---

## 🐛 Common Issues & Solutions

### Issue: Resume PDF not loading in production

**Solution:** Update PDF.js worker configuration:

```javascript
pdfjs.GlobalWorkerOptions.workerSrc = `https://unpkg.com/pdfjs-dist@${pdfjs.version}/build/pdf.worker.min.mjs`;
```

### Issue: Windows not draggable

**Solution:** Ensure GSAP Draggable is registered:

```javascript
import Draggable from "gsap/Draggable";
gsap.registerPlugin(Draggable);
```

### Issue: Hover animation too sensitive/not sensitive

**Solution:** Adjust intensity calculation in Welcome.jsx:

```javascript
const intensity = Math.exp(-(distance ** 2) / 20000); // Try 10000 or 30000
```

### Issue: Dock icons not animating

**Solution:** Check:

1. GSAP is imported correctly
2. `dock-icon` class is applied
3. Mouse events are attached in useGSAP hook

---

## 📚 Learning Resources

### Concepts Covered

- **React Patterns:** HOCs, Custom Hooks, Context
- **State Management:** Zustand with Immer
- **Animations:** GSAP, Draggable interactions
- **CSS:** Tailwind, Dynamic classes with CLSX
- **File Structure:** Scalable architecture
- **Performance:** Memoization, lazy loading

### Recommended Next Steps

1. **Add Features:**

   - Dark mode toggle
   - Mobile responsive design
   - Search functionality
   - More windows (Notes, Calendar)

2. **Optimize:**

   - Code splitting
   - Image optimization
   - Lazy load windows

3. **Test:**

   - Unit tests with Vitest
   - E2E tests with Playwright

4. **Enhance:**
   - Add sound effects
   - Keyboard shortcuts
   - Window minimize/maximize

---

## 🤝 Code Quality Tools

### Code Rabbit CLI

Install and use for automated code reviews:

```bash
# Install
curl -fsSL https://coderabbit.ai/install.sh | sh

# Login
coderabbit login

# Review changes
coderabbit
```

### GitHub Integration

1. Create `.github/workflows/coderabbit.yml`
2. Enable Code Rabbit in your repo
3. Get automated PR reviews

---

## 📖 Best Practices Applied

1. **Separation of Concerns**

   - Components for UI
   - Stores for state
   - Constants for data
   - HOCs for shared behavior

2. **Reusability**

   - WindowWrapper HOC
   - WindowControls component
   - Consistent patterns

3. **Clean Code**

   - Descriptive names
   - Single responsibility
   - DRY principle

4. **Performance**

   - Memoized callbacks
   - Event listener cleanup
   - Efficient re-renders

5. **Accessibility**
   - Semantic HTML
   - ARIA labels
   - Keyboard navigation support

---

## 🎯 Project Checklist

### Before Deployment

- [ ] Update all personal information
- [ ] Replace placeholder images
- [ ] Upload your resume PDF
- [ ] Test all windows open/close
- [ ] Verify drag functionality
- [ ] Check responsive behavior
- [ ] Test all external links
- [ ] Optimize images
- [ ] Run production build
- [ ] Test deployed site

### After Deployment

- [ ] Share on social media
- [ ] Add to LinkedIn projects
- [ ] Update GitHub README
- [ ] Submit to portfolio galleries
- [ ] Gather feedback
- [ ] Iterate and improve

---

## 💡 Tips for Success

1. **Stand Out:** This portfolio is already unique—make it yours
2. **Tell Your Story:** Use the About section effectively
3. **Showcase Best Work:** Quality over quantity in projects
4. **Keep Updated:** Regularly add new projects and skills
5. **Get Feedback:** Ask developers/recruiters for input
6. **Optimize Performance:** Fast load times matter
7. **Mobile Consideration:** Add mobile-friendly message
8. **SEO Matters:** Add meta tags for better discoverability

---

## 🔗 Useful Links

- **GSAP Documentation:** https://gsap.com/docs/
- **Zustand:** https://github.com/pmndrs/zustand
- **Tailwind CSS:** https://tailwindcss.com/docs
- **React PDF:** https://react-pdf.org/
- **Hostinger:** https://hostinger.com

---

## 📄 License

This project structure is for educational purposes. Feel free to use it for your personal portfolio.

---

## 🙏 Credits

Tutorial by: **JavaScript Mastery**

- YouTube: [@javascriptmastery](https://youtube.com/@javascriptmastery)
- Website: https://jsmastery.pro

---

## 🚀 Final Words

Remember: This portfolio isn't just about showing code—it's about showing **you can build experiences that matter**. Most developers have the same skills on paper. This is how you prove you're different.

Now go build something the internet won't forget! 🔥

---

**Built with ❤️ using React, GSAP, and Tailwind CSS**
